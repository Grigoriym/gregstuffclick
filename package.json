{
  "name": "gregstuffclick",
  "version": "1.0.0",
  "private": true,
  "description": "Personal website: resume, projects, about, blog.",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.mjs --dev",
    "build": "node esbuild.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^5.7.0"
  }
}
